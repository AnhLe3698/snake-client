const IP = "localhost"; // change to IP address of computer or ngrok host if tunneling
const PORT = 50541;
let keyBindings = ["w","a","s","d"];
let name = "AVL"; // three letters only

module.exports = {
  IP,
  PORT,
  keyBindings,
  name,
};